syntax = "proto3";

import "villas.proto";

package FMU_server;

service FMI_service {
    rpc FMU_Start (villas.node.Message) returns (villas.node.Message) {};
}

message Reference {
    repeated int32 ref = 1;
}

